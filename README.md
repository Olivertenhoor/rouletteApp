# rouletteApp
Depository for the company idea "altijdbij". Based on the idea of a free casino funded by adds on the side/casino games

## Usage


first run "npm install" in your terminal when in the right dir.
then run with: node app.js

dependencies:
    sudo apt install nodejs
    npm install
    npm install mysql2
